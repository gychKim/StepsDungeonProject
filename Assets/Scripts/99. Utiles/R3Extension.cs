using R3;
using System;
using UnityEngine.UI;

public static class R3Extension
{
	//public static IDisposable Subscribe<Unit>(this Observable<Unit> source, Action<Unit> onNext)
	//{
	//	return source.Subscribe(onNext);
	//}
}
